{
  "name": "creator-verse-x",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev --filter=@creatorverse/frontend",
    "lint": "turbo lint",
    "test": "turbo test",
    "clean": "turbo clean",
    "format": "turbo format"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
